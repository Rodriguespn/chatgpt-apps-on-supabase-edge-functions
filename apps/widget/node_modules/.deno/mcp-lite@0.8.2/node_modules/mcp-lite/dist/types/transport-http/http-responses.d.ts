import { type JsonRpcId } from "../types.js";
export declare function respondToInvalidJsonRpc(): Response;
export declare function respondToProtocolMismatch(responseId: JsonRpcId, protocolHeader: string, expected?: string | readonly string[]): Response;
export declare function respondToMissingProtocolHeader(responseId: JsonRpcId): Response;
/**
 * Responds with a 400 bad request if the session id is missing
 * @todo - we will want to make this response configurable, so someone can use a response format more tailored to their api conventions (https://github.com/fiberplane/mcp/issues/83)
 * @note - since this validaiton happens at the transport layer, we do not respond with a JSON-RPC error
 */
export declare function respondToMissingSessionId(): Response;
//# sourceMappingURL=http-responses.d.ts.map