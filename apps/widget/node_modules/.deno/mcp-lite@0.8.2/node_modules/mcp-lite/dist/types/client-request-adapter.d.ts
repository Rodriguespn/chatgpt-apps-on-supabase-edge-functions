export interface ClientRequestAdapter {
    /**
     * Create and track a pending client request. Returns a promise that
     * resolves when a matching JSON-RPC response is received (or rejects on timeout).
     */
    createPending(sessionId: string | undefined, requestId: string | number, options?: {
        timeout_ms?: number;
    }): {
        promise: Promise<unknown>;
    };
    /**
     * Resolve a pending request by providing the JSON-RPC response payload.
     * Returns true when a pending entry was found and resolved.
     */
    resolvePending(sessionId: string | undefined, requestId: string | number, response: unknown): boolean;
    /**
     * Reject a pending request (e.g., timeout/cancel). Returns true when a
     * pending entry was found and rejected.
     */
    rejectPending(sessionId: string | undefined, requestId: string | number, reason: unknown): boolean;
}
export declare class InMemoryClientRequestAdapter implements ClientRequestAdapter {
    private pending;
    private defaultTimeoutMs?;
    constructor(options?: {
        defaultTimeoutMs?: number;
    });
    createPending(sessionId: string | undefined, requestId: string | number, options?: {
        timeout_ms?: number;
    }): {
        promise: Promise<unknown>;
    };
    resolvePending(sessionId: string | undefined, requestId: string | number, response: unknown): boolean;
    rejectPending(sessionId: string | undefined, requestId: string | number, reason: unknown): boolean;
}
//# sourceMappingURL=client-request-adapter.d.ts.map